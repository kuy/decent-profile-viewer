name: Deploy infra [prd]

on:
  push:
    branches:
      - "infra-prd"

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
      - uses: kuy/decent-profile-viewer/.github/workflows/apply-tf-prd.yml@main
